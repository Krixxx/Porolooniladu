<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hepicode.porolooniladu">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FoamOutToBeDeletedActivity"></activity>
        <activity
            android:name=".FoamOutActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".FoamInToDeBeletedActivity" />
        <activity
            android:name=".FoamInDeletedActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".FoamInActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>